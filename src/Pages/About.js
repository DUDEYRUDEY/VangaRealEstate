import { AppBar, Toolbar, Typography, IconButton, Button } from '@mui/material';

function About(){
    return(
        <Typography>This is an about page about who we are</Typography>
    );
}

export default About;